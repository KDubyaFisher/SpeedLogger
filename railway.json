{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c \"python manage.py migrate && python manage.py collectstatic --noinput && gunicorn speedlogger_project.wsgi:application --bind 0.0.0.0:${PORT:-8000} --access-logfile - --error-logfile - --timeout 120\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}